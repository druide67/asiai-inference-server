# Engine manifest — llama.cpp auxiliary instance #3 — generic baseline
#
# Member of the llamacpp-aux-1/2/3/4 family. See ``llamacpp-aux-1.toml``
# for the family rationale and port assignment convention.
#
#   aisctl install llamacpp-aux-3 --preset <preset-name>

name = "llamacpp-aux-3"
display = "llama.cpp aux #3 (port 8092, small aux slot)"

[binary]
candidates = [
    "/opt/homebrew/bin/llama-server",
    "/usr/local/bin/llama-server",
]
process_pattern = "llms/gguf/aux3/active.gguf"
model_path = "~/llms/gguf/aux3/active.gguf"
program_args = [
    "--cont-batching",
    "--flash-attn", "on",
    "--mlock",
    "--n-gpu-layers", "999",
    "--jinja",
]
builds_from_source = false

[plist]
name = "com.asiai.llamacpp-aux-3"
throttle_interval = 10
timeout = 30

[network]
port = 8092
bind = "0.0.0.0"
health_endpoint = "/health"
health_timeout = 30

[firewall]
supported = true
anchor_name = "com.asiai.llamacpp-aux-3"

[logs]
dir = "~/Library/Logs/asiai/llamacpp-aux-3"
stdout = "llamacpp-aux-3.log"
stderr = "llamacpp-aux-3.err"

[wrapper]
needed = false

[environment]
vars = []
