"""asiai-inference-server core — engine lifecycle, manifests, memory, plist, sudoers, firewall.

This package provides the OS-level primitives that drive engine install, start,
stop, unload, and memory reclamation. It is intentionally stdlib-only and
macOS Apple Silicon only.
"""

__version__ = "0.3.0"
