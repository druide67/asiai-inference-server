"""asiai-inference-server CLI entry points.

Two faces:
  * `aisrv` — standalone binary (declared via [project.scripts]).
  * `asiai engine ...` — sub-CLI auto-injected into the asiai CLI via the
    `asiai.subcommands` entry point group.
"""
